
for (let index = 1; index <= 6; index++) {
   document.write(` <br>${index}\t `)
   for (let index2 = 1; index2 <= Math.pow(index, 2); index2++) {
      document.write(`${index2}\t`)
     
   }
   
}